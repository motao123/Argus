# 复制为 deploy/.env 后修改为高强度密码。
ARGUS_ADMIN_USER=admin
ARGUS_ADMIN_PASS=change-me-before-production
ARGUS_HTTP_PORT=8080
ARGUS_NAT_PORT=9090

# 可信反向代理（逗号分隔 IP/CIDR）。直连部署留空；nginx 反代时填代理地址，
# 否则 ClientIP 会记录代理而不是真实客户端。
ARGUS_TRUSTED_PROXIES=
